import { InidanCurrencyPipe } from './inidan-currency.pipe';

describe('InidanCurrencyPipe', () => {
  it('create an instance', () => {
    const pipe = new InidanCurrencyPipe();
    expect(pipe).toBeTruthy();
  });
});
