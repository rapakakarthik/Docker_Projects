import { RejectSpecialCharactersDirective } from './reject-special-characters.directive';

describe('RejectSpecialCharactersDirective', () => {
  it('should create an instance', () => {
    const directive = new RejectSpecialCharactersDirective();
    expect(directive).toBeTruthy();
  });
});
