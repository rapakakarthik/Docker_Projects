import { SafeImageDirective } from './safe-image.directive';

describe('SafeImageDirective', () => {
  it('should create an instance', () => {
    const directive = new SafeImageDirective();
    expect(directive).toBeTruthy();
  });
});
